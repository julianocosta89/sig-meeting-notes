## Meeting Notes

### Agenda
- [arthursens] The Collector SIG is asking for the stability of prometheus-translated metric names generated by all OTel Collector components because once they release 1.0 they cannot have names change. We have a long way to go, but we can help them avoid being caught by surprise: [https://github.com/open-telemetry/opentelemetry-collector/issues/13456](https://github.com/open-telemetry/opentelemetry-collector/issues/13456). How could we create an automation like this?
  - Client_golang testutil package?
  - Weaver?
  - [https://github.com/open-telemetry/opentelemetry-collector/pull/13519](https://github.com/open-telemetry/opentelemetry-collector/pull/13519)
  - A big problem here is the combination of two issues: they want to check if the declared otel metrics have not changed, and also too that the prometheus translations of those names has not changed.  And the twist is, they want unchanging metric names even when they are declared incorrectly. For instance, an otel metric is declared with a unit in the name (counter to the spec), and then the translator correctly appends the unit, causing a doubling. We had to write a workaround to remove the extra suffix to fix their metric.  (And we can't fix the metric because that would break anyone using the native (incorrect) otel name)
  - The issue of otlptranslator behavior will be solved by unit tests.  The issue of otel name stability would have to be installed by some sort of test that can collect all metrics by all stable components.
  - So how do you know which components are stable?
  - Also, some components have dynamic labels.
  - We really really need translation strategy configuration in otel-go because that's going to  break things again
  - Require some additional data for components to specify what metrics need stability?
  - Krajo: static analysis to find metric definitions and generate test?
- [arthursens] [Bumping Prometheus lib in collector got stuck with breaking changes](https://github.com/open-telemetry/opentelemetry-collector-contrib/pull/41318) again, anyone would like to pick this up?
  - Krajo to take a look Friday or early next week pending [https://github.com/prometheus/prometheus/pull/16951](https://github.com/prometheus/prometheus/pull/16951) getting to review state.
- [Juraj] Could use some feedback on what is mandatory before we ask people to test RW2 in the remote write exporter (tracking issue [https://github.com/open-telemetry/opentelemetry-collector-contrib/issues/33661](https://github.com/open-telemetry/opentelemetry-collector-contrib/issues/33661))
- [arthur] Bartek and Jonathan will do a mentorship about “native summaries”, which will eventually help translating Prometheus summaries into OTLP summaries.
  - [https://github.com/prometheus/prometheus/issues/16581](https://github.com/prometheus/prometheus/issues/16581) <- this is a related issue about memory handling to the issue I talked about which is that we have a large number of places in Prometheus where the code needs to branch 3 ways to cover floats/integer native histograms/float native histograms. (There’s actually a place in the WAL where we branch 5 ways). This is very ugly and ideally we’d have code that branches between floats and complex types including native summaries . And use some kind of dynamic type/union/whatever to reduce the branches.
